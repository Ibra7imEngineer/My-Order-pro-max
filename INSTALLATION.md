<!-- دليل التثبيت والتشغيل - My Order -->

# 🚀 دليل التثبيت والتشغيل

## المتطلبات الأساسية

قبل البدء، تأكد من توفر:

### 1. خادم ويب محلي
- **XAMPP** (الخيار الأسهل) - [تحميل XAMPP](https://www.apachefriends.org/)
- أو **WAMP**
- أو **MAMP** (لـ Mac)

### 2. البرامج المطلوبة
- **Apache** - لخدمة الملفات
- **MySQL** - لقاعدة البيانات
- **PHP 7.4 أو أعلى** - لمعالجة الكود

### 3. متصفح حديث
- Chrome أو Firefox أو Edge

---

## خطوات التثبيت

### ✅ الخطوة 1: تثبيت XAMPP

1. انتقل إلى [موقع XAMPP](https://www.apachefriends.org/)
2. حمّل النسخة الخاصة بنظام التشغيل الخاص بك
3. ثبّت البرنامج (اتبع التعليمات البسيطة)

### ✅ الخطوة 2: نقل الملفات

1. افتح مجلد XAMPP (غالباً في `C:\xampp`)
2. اذهب إلى مجلد `htdocs`
3. انسخ مجلد "My Order pro" كاملاً هنا

```
C:\xampp\htdocs\
├── My Order pro\
│   ├── index.html
│   ├── script-new.php
│   ├── style.css
│   ├── db-connect.php
│   ├── api-*.php
│   ├── schema.sql
│   └── ... (باقي الملفات)
```

### ✅ الخطوة 3: بدء الخوادم

1. افتح **XAMPP Control Panel**
2. اضغط **Start** بجانب **Apache**
3. اضغط **Start** بجانب **MySQL**

يجب أن تصبح كلاهما أخضر اللون.

### ✅ الخطوة 4: إنشاء قاعدة البيانات

#### الطريقة الأولى: عبر phpMyAdmin (الأسهل)

1. افتح متصفحك وانتقل إلى: `http://localhost/phpmyadmin`
2. اضغط على **"استيراد"** (Import) من الأعلى
3. اضغط **"اختر الملف"** واختر `schema.sql`
4. اضغط **"تنفيذ"**

✅ تمت إنشاء قاعدة البيانات بنجاح!

#### الطريقة الثانية: عبر Command Line

إذا كنت تفضل سطر الأوامر:

```bash
# على Windows - افتح Command Prompt
cd C:\xampp\mysql\bin
mysql -u root -p < "C:\xampp\htdocs\My Order pro\schema.sql"
# اضغط Enter عندما يطلب كلمة المرور (لا تدخل أي شيء إذا كانت فارغة)
```

### ✅ الخطوة 5: تفعيل الموقع

الآن افتح متصفحك وانتقل إلى:

```
http://localhost/My%20Order%20pro/
```

أو

```
http://localhost/"My Order pro"/
```

---

## ✅ اختبار التثبيت

### الصفحة الرئيسية

1. يجب أن ترى الصفحة الرئيسية مع المنتجات
2. جرب البحث عن منتج
3. جرب إضافة منتج للسلة

### تسجيل دخول الإدارة

1. اضغط على أيقونة **الترس** ⚙️ في الأعلى
2. اضغط على **تسجيل الدخول**
3. استخدم البيانات:
   - **البريد**: `admin@myorder.com`
   - **كلمة المرور**: `admin123`

### إضافة منتج جديد

1. في لوحة الإدارة، أدخل اسم الوجبة والسعر
2. اختر الفئة
3. أضف رابط الصورة أو اختر ملف
4. اضغط "إضافة الآن"

---

## 🔧 استكشاف الأخطاء الشائعة

### ❌ الخطأ: "لا يمكن الاتصال بالخادم"

**الحل:**
```
1. تأكد من تشغيل Apache و MySQL في XAMPP
2. تأكد من وضع الملفات في C:\xampp\htdocs\
3. حاول تشغيل الخادم بمسؤول (Admin)
```

### ❌ الخطأ: "خطأ في الاتصال بقاعدة البيانات"

**الحل:**
```
1. تأكد من تشغيل MySQL من XAMPP
2. افتح phpMyAdmin تأكد من وجود قاعدة البيانات my_order_db
3. إذا لم توجد، استورد ملف schema.sql مرة أخرى
```

### ❌ الخطأ: "404 Not Found"

**الحل:**
```
1. تأكد من الرابط: http://localhost/My%20Order%20pro/
2. احذف المسافات من اسم المجلد (غيّره إلى MyOrderPro)
3. أعد تسمية المسار بدون مسافات
```

### ❌ الخطأ: "ERR_INVALID_URL"

**الحل:**
```
الرابط الصحيح: http://localhost/My%20Order%20pro/
لا تستخدم: http://localhost/MyOrderPro
(المسافات يجب تحويلها إلى %20)
```

---

## 📱 اختبار على الهاتف

لتجربة الموقع على هاتفك أو جهاز آخر:

### الخطوة 1: معرفة IP الكمبيوتر
```bash
# على Windows - افتح Command Prompt واكتب:
ipconfig
# ابحث عن "IPv4 Address" مثل 192.168.1.100
```

### الخطوة 2: الدخول من الهاتف
```
http://192.168.1.100/My%20Order%20pro/
```

(استبدل 192.168.1.100 بـ IP كمبيوترك)

---

## 🎯 الخطوات التالية

بعد النجاح في التثبيت:

1. ✅ جرب إنشاء حساب جديد
2. ✅ أضف بعض المنتجات من لوحة الإدارة
3. ✅ جرب إنشاء طلب
4. ✅ أرسل رسالة تواصل
5. ✅ أضف تقييم لمنتج

---

## 📚 موارد إضافية

- [توثيق PHP](https://www.php.net/docs.php)
- [توثيق MySQL](https://dev.mysql.com/doc/)
- [HTML و CSS](https://www.w3schools.com/)

---

## 💡 نصائح مهمة

1. **النسخ الاحتياطية**: احفظ نسخة من قاعدة البيانات بانتظام
2. **التحديثات**: حافظ على تحديث PHP و MySQL
3. **الأمان**: غيّر كلمات المرور الافتراضية في الإنتاج
4. **الأداء**: استخدم قاعدة بيانات منفصلة للإنتاج

---

## ✨ مبروك! 🎉

أنت الآن جاهز لاستخدام My Order!

للمساعدة الإضافية، راجع ملف `README.md`

---

**آخر تحديث: 2026-01-31**
