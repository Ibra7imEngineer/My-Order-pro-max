# 🍔 My Order - منصة طلب الطعام الاحترافية

## 📋 نظرة عامة

**My Order** هو موقع ويب متكامل لإدارة طلبات الطعام بسهولة واحترافية. يوفر للعملاء تجربة سلسة لتصفح المنتجات وطلبها، مع لوحة تحكم قوية للإدارة.

---

## ✨ المميزات الرئيسية

### للعملاء:
- ✅ تصفح المنتجات (أطعمة، مشروبات، حلويات)
- ✅ بحث متقدم عن الأصناف
- ✅ سلة مشتريات ديناميكية
- ✅ تسجيل دخول وإنشاء حساب
- ✅ إدارة الطلبات والتاريخ
- ✅ تقييم وتقديم الآراء
- ✅ نموذج تواصل للاستفسارات
- ✅ موقع متجاوب مع جميع الأجهزة

### للإدارة:
- ✅ لوحة تحكم شاملة
- ✅ إضافة وتعديل وحذف المنتجات
- ✅ إدارة الطلبات
- ✅ عرض جميع التقييمات
- ✅ إدارة رسائل التواصل

---

## 🛠️ التقنيات المستخدمة

### Frontend:
- **HTML5** - الهيكل الأساسي
- **CSS3** - التصميم والتأثيرات
- **JavaScript** - التفاعل والديناميكية
- **Font Awesome** - الرموز والأيقونات

### Backend:
- **PHP** - المعالجة الخلفية
- **MySQL** - قاعدة البيانات

---

## 📦 الملفات الأساسية

```
My Order pro/
├── index.html              # الصفحة الرئيسية
├── style.css              # أنماط التصميم
├── script-new.php         # الأسكريبت الرئيسي (JavaScript)
├── db-connect.php         # اتصال قاعدة البيانات
├── api-products.php       # API المنتجات
├── api-users.php          # API المستخدمين
├── api-orders.php         # API الطلبات
├── api-reviews.php        # API التقييمات
├── schema.sql             # هيكل قاعدة البيانات
├── firebase-config.js     # إعدادات Firebase (اختياري)
├── db-seed.js             # بيانات تجريبية
└── README.md              # هذا الملف
```

---

## 🚀 خطوات التثبيت والتشغيل

### 1️⃣ متطلبات النظام
- **XAMPP** أو **Wamp** أو أي خادم Apache/MySQL محلي
- **PHP 7.4+**
- **MySQL 5.7+**
- **متصفح حديث**

### 2️⃣ تنزيل واستخراج الملفات
```bash
# استخرج جميع الملفات في مجلد htdocs (XAMPP) أو www (Wamp)
My Order pro/
```

### 3️⃣ إنشاء قاعدة البيانات

#### الطريقة الأولى: عبر phpMyAdmin
1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. انقر على "استيراد" (Import)
3. اختر ملف `schema.sql`
4. انقر "تنفيذ"

#### الطريقة الثانية: عبر MySQL Command Line
```bash
mysql -u root -p < schema.sql
```

### 4️⃣ تحديث إعدادات قاعدة البيانات
عدّل معلومات الاتصال في `db-connect.php` إذا لزم الأمر:

```php
define('DB_HOST', 'localhost');  // عنوان الخادم
define('DB_USER', 'root');       // اسم المستخدم
define('DB_PASS', '');           // كلمة المرور
define('DB_NAME', 'my_order_db'); // اسم قاعدة البيانات
```

### 5️⃣ تشغيل الموقع
```
http://localhost/My%20Order%20pro/
```

---

## 📖 دليل الاستخدام

### للعملاء:

#### تسجيل حساب جديد:
1. اضغط على أيقونة المستخدم في الأعلى
2. اختر "تسجيل حساب جديد"
3. ملء البيانات المطلوبة (البريد الإلكتروني، كلمة المرور، إلخ)
4. اضغط تأكيد

#### إنشاء طلب:
1. تصفح المنتجات من قائمة المنيو
2. اضغط "أضف للسلة" على المنتجات المرغوبة
3. اضغط على أيقونة السلة للانتقال لملخص الطلب
4. ملء عنوان التوصيل والهاتف
5. اختر طريقة الدفع
6. اضغط "أكمل عبر واتساب"

#### تقييم المنتجات:
1. انتقل إلى صفحة "تواصل معنا"
2. املء نموذج التقييم
3. اضغط إرسال

### للإدارة:

#### الدخول لوحة التحكم:
1. اضغط على أيقونة الترس (Settings) في الأعلى
2. اضغط على أيقونة تسجيل الدخول
3. البيانات الافتراضية:
   - **المستخدم**: `admin@myorder.com`
   - **كلمة المرور**: `admin123`

#### إضافة منتج:
1. في لوحة التحكم، أدخل اسم الوجبة والسعر والصورة
2. اختر الفئة (طعام/مشروب/حلوى)
3. اضغط "إضافة الآن"

#### إدارة الطلبات:
1. عرض جميع الطلبات الجديدة
2. تحديث حالة الطلب (جديد، قيد المعالجة، في الطريق، تم التسليم)

---

## 🔐 بيانات الدخول الافتراضية

### حساب الإدارة:
```
البريد: admin@myorder.com
كلمة المرور: admin123
```

---

## 📊 هيكل قاعدة البيانات

### جدول المستخدمين (users)
- `id` - معرف المستخدم
- `name` - الاسم الكامل
- `email` - البريد الإلكتروني
- `password` - كلمة المرور (مشفرة)
- `phone` - رقم الهاتف
- `address` - العنوان
- `is_admin` - هل هو إداري

### جدول المنتجات (products)
- `id` - معرف المنتج
- `name` - اسم المنتج
- `description` - الوصف
- `price` - السعر
- `category` - الفئة (food/drinks/sweets)
- `image_url` - رابط الصورة
- `is_available` - متاح للطلب أم لا

### جدول الطلبات (orders)
- `id` - معرف الطلب
- `user_id` - معرف المستخدم
- `total_price` - السعر الإجمالي
- `shipping_fee` - رسوم التوصيل
- `payment_method` - طريقة الدفع
- `order_status` - حالة الطلب
- `delivery_address` - عنوان التوصيل

### جدول تفاصيل الطلبات (order_items)
- `id` - معرف البند
- `order_id` - معرف الطلب
- `product_id` - معرف المنتج
- `quantity` - الكمية
- `price` - السعر

### جدول التقييمات (reviews)
- `id` - معرف التقييم
- `user_id` - معرف المستخدم
- `product_id` - معرف المنتج
- `rating` - التقييم (1-5)
- `comment` - الرأي

### جدول رسائل التواصل (contact_messages)
- `id` - معرف الرسالة
- `name` - الاسم
- `email` - البريد الإلكتروني
- `subject` - الموضوع
- `message` - نص الرسالة
- `is_read` - هل تمت قراءتها

---

## 🔧 استكشاف الأخطاء

### خطأ: "خطأ في الاتصال بقاعدة البيانات"
- تأكد من أن MySQL يعمل
- تحقق من بيانات الاتصال في `db-connect.php`
- تأكد من وجود قاعدة البيانات `my_order_db`

### خطأ: "الملف غير موجود 404"
- تأكد من أن جميع الملفات في نفس المجلد
- تحقق من أسماء الملفات (حساس لحالة الأحرف على Linux)

### خطأ: "CORS" عند الطلب
- تأكد من أن الموقع يعمل على نفس النطاق
- فعّل CORS في `db-connect.php` (مفعل بالفعل)

---

## 📱 الاستجابة والتصميم

الموقع مصمم ليكون متجاوباً ويعمل بشكل مثالي على:
- ✅ الهواتف الذكية (Mobile)
- ✅ الأجهزة اللوحية (Tablet)
- ✅ أجهزة الكمبيوتر (Desktop)

---

## 🎨 التخصيص

### تغيير الألوان الأساسية:
عدّل المتغيرات في `style.css`:
```css
:root {
    --primary: #FF6B35;        /* اللون الأساسي (البرتقالي) */
    --primary-dark: #E85A1F;   /* الأغمق */
    --primary-light: #FF8E5F;  /* الأفتح */
    /* ... باقي الألوان ... */
}
```

### تغيير معلومات المتجر:
عدّل البيانات في `index.html` والملفات ذات الصلة

---

## 📞 الدعم والتواصل

للمساعدة والدعم الفني:
- 📧 البريد: `admin@myorder.com`
- 📱 الواتساب: `+201021279663`

---

## 📜 الترخيص

جميع الحقوق محفوظة © 2026 My Order

---

## 🎯 النقاط المستقبلية (Roadmap)

- [ ] تطبيق موبايل (iOS و Android)
- [ ] نظام دفع إلكترونية متكامل
- [ ] نظام تقييم النقاط والمكافآت
- [ ] نظام البريد الإلكتروني التلقائي
- [ ] تقارير متقدمة وإحصائيات
- [ ] نظام توصيل متطور مع خريطة

---

**تم إنشاء هذا المشروع بحب وشغف بالتطوير ❤️**
